// Script Node autonome, hors Playwright (QA_PLAN.md section 2).
// `lighthouse` n'est pas encore installé (réservé à la tâche Performance,
// QA_PLAN.md section 3) : ce fichier est un stub tant que la dépendance
// n'est pas ajoutée.

console.error(
  "test:qa:perf indisponible : la dépendance 'lighthouse' n'est pas encore installée (voir QA_PLAN.md section 3).",
);
process.exit(1);
