import { test } from "playwright/test";

// sitemap.ts/robots.ts n'existent pas dans src/app (QA_PLAN.md section 1).
test.fixme("sitemap: /sitemap.xml accessible et valide", async () => {});
test.fixme("robots: /robots.txt accessible et valide", async () => {});
