import { test } from "playwright/test";

test.fixme("metadata: title/description définis sur la home", async () => {});
test.fixme("metadata: title/description définis sur project-page", async () => {});
// Absence d'opengraph-image constatée dans QA_PLAN.md section 1 (gap connu).
test.fixme("metadata: balises Open Graph présentes", async () => {});
